import {
  sum,
  powerOf2,
  subtract,
  multiply,
  divide
} from "./math";

import * as Blah from './math';

console.log( powerOf2(99) );

console.log( sum(21, 21) );

console.log( Blah.randomInteger(100) );
